/** Legacy planning routes remain available inside the shared dark shell. */
export const viewport = { themeColor: "#000000" };

export default function AtomikLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
